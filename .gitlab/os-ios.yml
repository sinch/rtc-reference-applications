.conf:ios:runner:tags:
  tags:
    - rtc-ci-macbook

.lint:ios:do:
  script:
    - swiftlint --strict

.build:ios:do:
  script:
    - xcodebuild clean
    - xcodebuild -scheme SinchReferenceApp

  interruptible: true
