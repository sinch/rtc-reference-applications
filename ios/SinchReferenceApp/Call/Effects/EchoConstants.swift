import UIKit

enum EchoConstants {
  static let localActiveColor = UIColor(red: 0.2, green: 0.6, blue: 0.86, alpha: 1)
  static let remoteActiveColor = UIColor(red: 0.87, green: 0.35, blue: 0.62, alpha: 1)

  static let localOnMessage = "Local echo on — remote party hears your echo"
  static let localOffMessage = "Local echo off"
  static let remoteOnMessage = "Remote echo on — you hear the remote party's echo"
  static let remoteOffMessage = "Remote echo off"
}
