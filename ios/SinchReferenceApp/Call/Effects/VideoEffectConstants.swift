import UIKit

enum VideoEffectConstants {
  static let localActiveColor = UIColor.systemGreen
  static let remoteActiveColor = UIColor.systemPurple

  static let localOnMessage = "Local black & white on — remote party sees your video in black & white"
  static let localOffMessage = "Local black & white off"
  static let remoteOnMessage = "Remote black & white on — you see the remote party's video in black & white"
  static let remoteOffMessage = "Remote black & white off"
}
